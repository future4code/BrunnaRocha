import { ClientOrder } from '../../entities/ClientOrder'
import {generateRandomId} from '../../utils/generateRandomId'
import {CalculatePriceDatabase} from '../../../data/CalculatePriceDatabase'

export class CalculatePriceInteractor {
  private calculatePriceDatabase: CalculatePriceDatabase
  }
