/**
 *
 * Implemente um método que performe uma compressão de string
 * usando a contagem dos caracteres repetidos.
 *
 */

export const stringCompression = (input: string): string => {
  return "";
};
